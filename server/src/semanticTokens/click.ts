import { ClickTree as Tree } from '../parser';
import { ParsedToken } from "../semanticTokens";

export function getTokens(tree: Tree): ParsedToken[] {
	const tokens: ParsedToken[] = [];
	return tokens;
}